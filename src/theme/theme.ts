// theme.ts

const colors = {
  primary: "#820AD1", // Roxo Nubank
  primaryDark: "#5C0AA3",
  primaryLight: "#B967FF",

  secondary: "#FFD100", // Amarelo para a temática pet (pata)
  secondaryDark: "#C2A200",

  textPrimary: "#1A1A1A",
  textSecondary: "#4D4D4D",
  background: "#FFFFFF",
  surface: "#F9F9F9",
  error: "#E53935",
};

const fontSizes = {
  xs: 12,
  sm: 14,
  base: 16,
  lg: 18,
  xl: 24,
  xxl: 32,
};

const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

const borderRadius = {
  sm: 4,
  md: 8,
  lg: 16,
  pill: 999,
};

const fonts = {
  light: "Font-Light",
  regular: "Font-Regular",
  bold: "Font-Bold",
};

const theme = {
  colors,
  fontSizes,
  spacing,
  borderRadius,
  fonts,
};

export default theme;
